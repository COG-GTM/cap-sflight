{
    "id": "cds-dk",
    "version": "1.0.0",
    "name": "cds-dk",
    "description": "Install @sap/cds-dk (via npm)",

    "options": {

        "version": {
            "type": "string",
            "proposals": [
                "10.1.1"
            ],
            "default": "10.1.1",
            "description": "Exact @sap/cds-dk version to install (ranges and 'latest' are rejected)"
        }
    },

    "dependsOn": {
        "ghcr.io/devcontainers/features/node:1": { }
    }
}