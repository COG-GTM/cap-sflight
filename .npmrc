# Dependency lifecycle scripts (preinstall/install/postinstall) are not run on
# install. Packages that genuinely need a build/download step are rebuilt
# explicitly via `npm run rebuild:trusted`.
ignore-scripts=true
